export const metadata = {
  title: 'Sign In — Dentwise',
  description: 'Sign in to your Dentwise account to manage dental appointments and AI consultations.',
}

export default function AuthLayout({ children }) {
  return children
}
